name: flink-upload
version: "1.0"
type: action
category: data_management
displayName: ${{ i18n.displayName }}
desc: ${{ i18n.desc }}
logoUrl:
imageUrls:
public: true
supportedVersions: # Deprecated. Please use supportedErdaVersions instead.
  - ">= 3.5"
supportedErdaVersions:
  - ">= 1.0"

labels:
  maintainer: cb167668@alibaba-inc.com

source:
- name: jobManagerUrl
  required: true
  desc: flink cluster jobManagerUrl
  default:
- name: jarPath
  required: true
  desc: flink jar path
  default:
locale:
  zh-CN:
    desc: Flink上传 Action允许用户上传一个Flink作业的jar包
    displayName: Flink上传
