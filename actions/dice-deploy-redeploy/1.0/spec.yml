name: dice-deploy-redeploy
version: "1.0"
type: action
category: deploy_management
desc: 用于 dice 平台部署应用服务
public: true
supportedVersions: # Deprecated. Please use supportedErdaVersions instead.
  - ">= 3.19"
supportedErdaVersions:
  - ">= 1.0"

params:

locale:
  zh-CN:
    displayName: "dice-deploy-redeploy"
    desc: 用于 dice 平台部署应用服务
  en-US:
    displayName: "dice-deploy-redeploy"
    desc: "Used for dice platform deployment application service"

accessibleAPIs:
  # redeploy
  - path: /api/runtimes/<runtimeId>/actions/redeploy-action
    method: POST
    schema: http

outputs:
  - name: runtime_id
    desc: 部署中心运行时 ID
  - name: deployment_id
    desc: 部署单 ID
