name: wait
version: '1.0'
type: action
displayName: 等待
desc: 执行等待操作
public: true
executor:
  kind: "WAIT"
  name: "wait"
supportedVersions:
  - ">= 4.0"
supportedErdaVersions:
  - ">= 1.2"

params:
  - name: wait_time_sec
    required: true
    desc: 等待时间(秒)
