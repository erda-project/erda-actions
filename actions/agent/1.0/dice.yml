### job 配置项
jobs:
  agent:
    image: registry.erda.cloud/erda-actions/action-agent:1.6-alpha-20211222175630-22f1e31d
    labels:
      # 必填字段
      MD5: e1acc05920c0ad7347989b234dba30de
    envs:
      # DEBUG mode, will produce more logs
      DEBUG: false
      # push log to collector
      ENABLE_PUSH_LOG_TO_COLLECTOR: false
      STDERR_REGEXP_LIST: '["^[a-z]*can*","^[a-z]*fail*"]'
      ENCRYPTED_VAlUE_MIN_LEN: 6
